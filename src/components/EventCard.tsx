import type { TimeEvent } from "../types/Event"

type EventCardProps = {
  data: TimeEvent
}

const EventCard = ({data}: EventCardProps) => {
  return (
    <div>
      <header>
        <img src="" alt="" />
      </header>
    </div>
  )
}

export default EventCard